import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Vendors",
};

/** Route segment for /vendors — UI is rendered by (admin)/layout → App. */
export default function VendorsPage() {
  return null;
}
