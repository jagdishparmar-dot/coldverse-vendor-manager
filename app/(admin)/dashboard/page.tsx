import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
};

/** Route segment for /dashboard — UI is rendered by (admin)/layout → App. */
export default function DashboardPage() {
  return null;
}
