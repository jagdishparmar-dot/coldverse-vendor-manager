import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings",
};

/** Route segment for /settings — UI is rendered by (admin)/layout → App. */
export default function SettingsPage() {
  return null;
}
