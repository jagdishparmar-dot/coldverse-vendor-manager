import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Archive",
};

/** Route segment for /archive — UI is rendered by (admin)/layout → App. */
export default function ArchivePage() {
  return null;
}
