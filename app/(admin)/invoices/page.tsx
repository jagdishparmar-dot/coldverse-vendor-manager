import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Invoices",
};

/** Route segment for /invoices — UI is rendered by (admin)/layout → App. */
export default function InvoicesPage() {
  return null;
}
