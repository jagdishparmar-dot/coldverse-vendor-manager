import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "KYC",
};

/** Route segment for /kyc — UI is rendered by (admin)/layout → App. */
export default function KycPage() {
  return null;
}
