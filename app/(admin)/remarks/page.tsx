import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Remarks",
};

/** Route segment for /remarks — UI is rendered by (admin)/layout → App. */
export default function RemarksPage() {
  return null;
}
